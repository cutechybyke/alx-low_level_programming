#include "main.h"

/**
 * swap_int - swaps the value of two given integers
 * @a: integer to swap
 * @b: integer to also swap
 */

void swap_int(int *a, int *b)
{
	int a;

	m = *a;
	*a = *b;
	*b = *m;
}
